<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.pofo.pofoapiboot3.repository.NotificationRepository">
    <insert id="insert">
        INSERT INTO
            notification
        VALUES
            (DEFAULT, #{typeId}, #{fromMemberId}, #{toMemberId}, #{commentId}, DEFAULT, DEFAULT)
    </insert>

    <delete id="delete">
        DELETE FROM
            notification
        WHERE
            type_id = #{typeId}
        AND
            from_member_id = #{fromMemberId}
        AND
            to_member_id = #{toMemberId}
        <choose>
            <when test="commentId != null">
                AND
                    comment_id = #{commentId}
            </when>
            <otherwise>
                AND
                    comment_id IS NULL
            </otherwise>
        </choose>
    </delete>
</mapper>